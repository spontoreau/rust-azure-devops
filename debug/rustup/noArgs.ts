import debug from "../common/debug";

const rustupCommandInput = {
  name: "rustupCommand",
  value: "show"
};

const verbose = {
  name: "verbose",
  value: "true"
};

debug("rustup.js", rustupCommandInput, verbose);
